{
  "name": "@trickle/example-web-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Live browser demo: authorize a budget with a passkey, then stream pay-per-second video on CKB Fiber (with an escrow sibling)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@trickle/auth": "workspace:*",
    "@trickle/core": "workspace:*",
    "@trickle/stream": "workspace:*"
  },
  "devDependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
